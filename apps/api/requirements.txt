# mind-sync API — Python 3.12+（生产镜像使用 3.12-slim）
# 安装：pip install -r apps/api/requirements.txt
# 或仓库根目录：pip install -r requirements.txt
#
# 说明：
# - starlette 显式锁定，满足安全审计（随 fastapi 升级时请一并检查）
# - pydantic / pydantic-core 为传递依赖；若 pip 尝试从源码编译 core，可改用：
#     pip install --only-binary=:all: pydantic-core
#     pip install -r apps/api/requirements.txt

fastapi==0.136.3
starlette==1.2.1
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
python-multipart==0.0.30
itsdangerous==2.2.0
PyYAML==6.0.2
httpx==0.28.1
trafilatura==2.0.0
bcrypt==4.2.1
jieba==0.42.1
